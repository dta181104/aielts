spring.application.name=shopmohinh
spring.datasource.url=jdbc:mysql://localhost:3306/aielts?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=none
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
server.port=8080
server.servlet.context-path=/identity

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Cloudinary Configuration
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME}
cloudinary.api_key=${CLOUDINARY_API_KEY}
cloudinary.api_secret=${CLOUDINARY_API_SECRET}

# Send Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

#GHN
ghn.api.token=${GHN_API_TOKEN}
ghn.api.url=https://dev-online-gateway.ghn.vn/shiip/public-api

# Redis Host & Port
spring.data.redis.host=localhost
spring.data.redis.port=6379     
spring.session.store-type=redis

# Allow bean definition overriding
spring.main.allow-bean-definition-overriding=true

# Gemini Configuration
gemini.api.key=${GEMINI_API_KEY}
gemini.api.timeout=30000
gemini.api.max-retries=3
gemini.api.model=gemini-2.5-flash
gemini.api.rate-limit.requests-per-minute=60
gemini.api.rate-limit.max-tokens=2048